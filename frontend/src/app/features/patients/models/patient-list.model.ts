export interface PatientListModel {
  patientID: number;
  firstName: string;
  lastName: string;
  dateOfBirth: string; // ISO date string (YYYY-MM-DD)
  genderName: string;
  contactNumber: string;
}

